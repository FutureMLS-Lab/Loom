# Handoff（2026-08-12 3:40 PM PT）

WACV rebuttal 质量整改 + AR paper 图片修复的交接存档。问题与解决方案的完整提纲见同目录
`REBUTTAL_QUALITY_FIXES_20260812.md`；本文只记录**当前状态与如何接手**。

## 1. 当前系统状态

- Loom Web：`127.0.0.1:8766`，公网 `https://p-0eed8292.gate.together-turbo.com`（tunnel 与鉴权 token 未变）。
- 服务代码：`rebuttal-factory` 分支。最后一次热重启在本日 08:30 UTC，已加载全部 delivery 质量门禁；
  之后仅新增了 `verify_delivery_figures` 的临时目录清理容错（只影响监控脚本进程，脚本每次冷启动加载，
  服务端无需立刻重启；下次有其他改动时顺带 `hot_restart.py` 即可）。
- Reviewer 面板：`gpt-5.6-sol-max-fast` + `claude-fable-5-thinking-max` + `cursor-grok-4.5-high-fast`
  （Cursor CLI），AR 评审与 delivery 图片验收共用。

## 2. 两篇 Rebuttal：都停在"等人工最终批准"

| | 2240 | 2237 |
|---|---|---|
| project id | `3c11ea8f8f58` | `e718e460ba64` |
| 交付 run | `20260812T083046Z-9df0b63d` | `20260812T083053Z-3de8dddc` |
| 状态 | `await_delivery_approval`，preflight + 三模型验收全过 | 同左 |
| 产物 | revised-paper 9p（正文满 8 页）+ rebuttal 1p（满页，彩色复述+编号证据，无 Revision boundary）+ supplement 6p | revised-paper 10p（正文满 8 页）+ rebuttal 1p（满页，四 reviewer 四色）+ supplement |

产物目录（`deliverables/`）：

```text
/data/shared/zhizhousha/workspace/loom-project/claude-paper/wacv2027/paper-2240/rebuttal-output/delivery/attempts/20260812T083046Z-9df0b63d/deliverables/
/data/shared/zhizhousha/workspace/loom-project/claude-paper/wacv2027/paper-2237/rebuttal-output/delivery/attempts/20260812T083053Z-3de8dddc/deliverables/
```

**接手动作**：人工审阅 PDF → Rebuttal Factory 页面对应 paper 点 `Final artifact approval`
（或 `POST /api/rebuttal/projects/<id>/approve-delivery`）→ 自动生成 `submission-bundle.zip`
→ 人工上传 OpenReview（系统绝不自动投稿）。

**注意**：批准前不要改动 paper source、responses 或 studio policy——任何改动都会使冻结摘要失效，
需要重跑 delivery。最终批准强制要求三模型图片验收通过且绑定当前 PDF 的 SHA-256，换了 PDF 必须重新验收。

## 3. 两篇 AR 生成 paper：本轮进行中

| | GUI Blindness Audit | MCD (Direction Matters) |
|---|---|---|
| 任务 slug | `wacv3-how-much-of-gui-agent-benchmark-...` | `wacv3-modality-contrastive-decoding-...` |
| 进度 | Round 4 进行中 | Round 5 进行中 |
| Author pane | `loom-claude-dcfb6755-wacv3-how-much-...:0.0` | `loom-claude-dcfb6755-wacv3-modality-...:0.0` |
| 图片修复 | Fig1 teaser 重生成（通栏两行布局，无压缩）；Fig2 稀疏图与 Fig3 重叠图例已被重构替换 | Fig1 重画（Okabe-Ito 配色、每 panel 一句话）；(c) 注释框移出绘图区，无重叠 |
| 待办 | 正文 analysis 图（MCD）本轮结束后复检 | 同左 |

PDF 实时路径（每轮原地重编译）：

```text
research-factory/.RUD/<slug>/work/manuscript/main.pdf
```

轮次收尾自动走 readiness gate（含 page-one 总览图检查）+ 三模型评审，无需人工介入；
Draft/Final Human Gate 到达时需要人工批准（此前的自动跳过脚本已结束，未再挂新的）。

## 4. 后台脚本与监控

- 通用交付监控脚本：`/data/shared/zhizhousha/.cursor-runtime/scripts/delivery_monitor.py`
  - 用法：`.venv/bin/python delivery_monitor.py <project_id> [own]`
  - 行为：等 Agent 完成标记 → 确定性 ingest/preflight → 三模型图片验收；失败自动把报告喂回
    Agent pane 迭代（上限 6 轮）；全过后打印 `DELIVERY_READY_FOR_HUMAN` 退出，**从不自动批准**。
  - `own` 参数含义：由脚本负责 ingest 完成标记（在绕过服务端 watcher、直接向 pane 发反馈的场景使用）。
- 当前无存活监控：两篇 rebuttal 的监控均以 READY_FOR_HUMAN 正常退出；AR PDF 重编译监视器亦已完成使命。
- 再次迭代 rebuttal 的两种方式：
  1. 全新 attempt：`POST /api/rebuttal/projects/<id>/rerun-delivery`，body 里 `feedback` 字段放自定义要求（本日新增能力）；
  2. 保留 Agent 工作区继续改：向 `loom-rebuttal-delivery-<id>:0.0` pane 发反馈 + 删除 marker +
     置 `stage=delivery_agent_running` 后跑 `delivery_monitor.py <id> own`（脚本 blocked 分支即此模式）。

## 5. 本日代码提交

- `1a8b3a9` Require unanimous figure sign-off before delivery approval（早期版本，条件触发）
- `ad1027e` Send author experiments to the GPU cluster（GPU-RESOURCES 技能）
- `54544fb` Merge `origin/zhongzhu/dev`（stall 唤醒、page-one 图 readiness、docs/ 重组等 8 commits，零冲突，297 测试全过）
- 本次提交：delivery 质量门禁强化（无条件图片验收 + SHA 绑定、正文满页确定性检查、模板质量条款、
  rerun-delivery 自定义反馈、验收临时目录容错）+ GPU 技能防御性 preflight + 本目录两份存档文档
- Reviewer 面板还原相关改动在更早的提交中（见 git log 08-12 早晨）

## 6. 已知残留事项

- MCD 正文 analysis 图：指令已在 Round 5 内，轮次结束后需人工复检一次。
- 2240 revised paper 正文未显式引用 supplement（不违规，WACV 分开上传；介意可在下轮迭代补一句）。
- `.RUD/`（仓库根）是运行时任务状态目录，保持 untracked，勿提交。
